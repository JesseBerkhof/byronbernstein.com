<div class="w-full mb-6">
    <textarea
        name="{{ $name }}"
        id="{{ $name }}"
        cols="30"
        rows="10"
        class="appearance-none block w-full bg-gray-800 text-gray-500 border border-gray-900 rounded py-3 px-4 leading-tight focus:outline-none focus:bg-gray-700 focus:border-gray-600" name="{{ $name }}"
        type="text"></textarea>
</div>
